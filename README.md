🏛 Полное техническое задание: JDU Cowork (Professional Version)
1. Общее описание системы
JDU Cowork — это высокопроизводительное веб-приложение для управления проектами и задачами, оптимизированное для студентов и профессионалов. Система поддерживает мультиязычность (UZ, JP), темную тему с эффектами Glassmorphism и real-time отслеживание дедлайнов.

2. Технологический стек
Frontend: React.js (Vite), Zustand (State Management), Tailwind CSS + Framer Motion (анимации), Material UI (сложные формы/компоненты), Lucide React (иконки).

Backend: Node.js, Express.js (REST API).

Database: PostgreSQL (реляционная структура, pg-pool).

Auth: JWT в HttpOnly Cookies, Bcrypt для хеширования.

Notifications: SMTP (Nodemailer) для триггерных рассылок.

Validation: Zod (Full-stack validation).

3. Архитектура базы данных (Naming Convention)
Правило: snake_case для БД, camelCase для API.

Таблица users

id: UUID (PK).

email: VARCHAR (Unique, Indexed).

password_hash: TEXT.

full_name, avatar_url: TEXT.

preferred_lang: VARCHAR ('uz', 'jp', 'en').

Таблица projects

id: UUID (PK).

owner_id: FK -> users.id.

title_uz, title_jp, title_en: VARCHAR.

desc_uz, desc_jp, desc_en: TEXT.

category: VARCHAR (Design, Development, Marketing, etc.).

color_code: VARCHAR (HEX для кастомизации).

is_public: BOOLEAN (Default: false).

Таблица tasks

id: UUID (PK).

project_id: FK -> projects.id.

title_uz, title_jp, title_en: VARCHAR.

deadline: TIMESTAMPTZ (Строго deadline, не due_date).

status: ENUM ('todo', 'in_progress', 'done', 'reviewed').

priority: ENUM ('low', 'mid', 'high').

assigned_to: FK -> users.id.

4. Функциональные модули
4.1. Аутентификация и Безопасность

Flow: Регистрация -> Логин -> Выдача JWT в HttpOnly Cookie.

Валидация: Email format, Name (min 2), Password (min 6).

Защита: CORS-политики, защита от XSS, валидация входящих дат (запрет дедлайнов в прошлом).

4.2. Dashboard (Аналитика)

Виджеты: Общее кол-во проектов, завершенные/ожидающие задачи.

Weekly Goal: Прогресс-бар выполнения недельной цели (напр. 32/40).

Activity Stream: Лента последних действий.

Hot Deadlines: Список задач, срок которых истекает в ближайшие 24-48 часов.

4.3. Управление Проектами

Grid View: Карточки с прогресс-баром, дедлайном и аватарами участников.

Создание: Атомарная операция — при создании проекта автор автоматически становится admin.

Фильтрация: Поиск по названию и категориям.

4.4. Task Manager (Kanban & List)

Канбан: Колонки (To Do, In Progress, Done, Reviewed).

DND: Интеграция @dnd-kit для перемещения задач с мгновенным обновлением статуса в БД.

Priority: Цветовая индикация (Красный — High, Желтый — Mid, Синий — Low).

4.5. Календарь

Библиотека: FullCalendar или MUI Date Grid.

Синхронизация: Все события привязаны к полю deadline из БД.

Интерактив: Добавление задачи кликом на дату.

5. Frontend & UX Нюансы
Мультиязычность: Мгновенное переключение через Context API (i18next).

Fallback Logic: Если title_jp пустое — отображать title_uz.

UI: Dark Mode по умолчанию, Glassmorphism Blur эффекты на боковой панели и модальных окнах.

Error Handling: Использование Sonner или React-Hot-Toast для уведомлений об ошибках (никаких белых экранов).

Interceptors: Axios автоматически прокидывает credentials и обрабатывает 401 ошибки.

6. План миграции и разработки
Phase 1 (Infrastructure): Инициализация Vite + Express. Настройка PostgreSQL и пула соединений.

Phase 2 (Auth & Schema): Запуск SQL-миграций. Реализация регистрации/логина.

Phase 3 (Core Logic): CRUD проектов и задач. Реализация атомарных операций и SMTP-триггеров.

Phase 4 (UI/UX): Верстка Dashboard и Kanban. Подключение i18next и темной темы.

Phase 5 (Integration): Синхронизация фронтенда с API через Axios Interceptors, финальное тестирование валидации Zod.
